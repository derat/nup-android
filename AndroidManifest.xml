<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.erat.nup"
    android:versionCode="1"
    android:versionName="1.0">
  <application android:label="@string/app_name"
      android:icon="@drawable/icon"
      android:debuggable="true">
    <service android:name=".NupService"
        android:exported="false"/>
    <activity android:name=".NupActivity"
        android:label="@string/app_name"
        android:launchMode="singleTask">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".BrowseActivity" />
    <activity android:name=".SearchActivity" />
    <activity android:name=".SearchResultsActivity" />
    <activity android:name=".SettingsActivity" />
  </application>

  <!-- TODO: I actually have no idea what the minimum version that this
       will run on is; I'm just using 4 since that gets rid of the
       implicit "Read phone state" privilege. -->
  <uses-sdk android:minSdkVersion="4" android:targetSdkVersion="9" />

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
</manifest>
